let a = prompt ('nhap a')
let b = prompt ('nhap b')
let c = prompt ('nhap c')
let delta
delta = b * b - 4 * a * c
if (delta < 0) {
    document.write ('phuong trinh vo nghiem')
} else {
    if (delta == 0) {
        let  -b * 1.0 / 2 * a
        document.write (' phương trinh co nghiem kep')
    }
}